# Changelog

本插件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本号遵循语义化版本。

## [v0.6.2] - 2026-08-09

### 修复
- **垃圾桶点击不再打开恢复 flyout**：块被拖入垃圾桶后即视为删除，点击垃圾桶不再弹出可恢复被删块的 flyout 面板（覆盖 `openFlyout` 方法从根本上阻止打开）。
- **导入不再覆盖已有程序**：导入时总为每个条目分配新的随机 `id` 且默认不启用，因此无论同名还是同 id 都不会覆盖已有程序。

### 变更
- **导出不再包含程序名称**：导出的 JSON 仅携带内容相关字段（`description`、`workspace`、`code`、`trigger` 等），程序命名由导入时自行设置，名称与已有程序冲突时拒绝导入。
- **导入命名必填**：导入确认弹窗中程序命名改为必填，且实时校验不能与已有程序同名；后端同样校验并拒绝冲突命名。

### 新增
- **编辑区顶部导出按钮**：工具栏新增「导出」按钮，可对当前程序单独导出。

### 优化
- **导入确认弹窗**：点击导入选择文件后弹出「导入确认」对话框，显示将导入的程序数量，可设置程序命名，并支持取消/确认导入；不再自动检测同名冲突。
- **导入后自动刷新编辑区**：导入完成后自动重新加载编辑区（若有未保存修改，先询问是否保存），无需手动重新进入插件界面。
- **程序描述改为按钮弹窗编辑**：设置栏的描述输入框改为按钮，点击后弹出多行文本弹窗进行描述书写。

## [v0.6.1] - 2026-08-09

### 新增
- **声明支持平台**：metadata.yaml 新增 `support_platforms` 字段，声明插件仅支持 OneBot（`aiocqhttp`）与 QQ 官方机器人（`qq_official`、`qq_official_webhook`）平台，用于 WebUI 插件页展示。

## [v0.6.0] - 2026-08-09

### 变更
- **项目全面重命名为 Blockly**：插件名、包名、仓库名由 `blocky` 修正为 `blockly`（`astrbot_plugin_blocky` → `astrbot_plugin_blockly`）。
- **积木类型标识前缀由 `blocky_` 改为 `blockly_`**：`blockly_event`、`blockly_reply`、`blockly_get_message` 等。注意：此前的旧程序文件中的积木类型无法被新版本识别，需在 WebUI 编辑器中重新创建。
- **聊天指令 `/blocky` 改为 `/blockly`**（别名 `blk` 保持不变）。
- **仓库地址变更为** `https://github.com/BUXIN-A/astrbot_plugin_blockly`。

## [v0.5.1] - 2026-08-08

### 修复
- **收纳盒常驻开关互动不完全修复**：Blockly 工具箱处理 `mousedown` 事件，此前 `stopPropagation` 绑在 `click` 上为时已晚，`mousedown` 早已冒泡到工具箱触发 `clearSelection`，导致常驻开启时 flyout 被关闭、关闭时 flyout 被打开。改为在 `mousedown` 上 `stopPropagation`，从源头阻断事件冒泡。
- **鼠标切换时不再同步处理分类选中**：`change` 事件在鼠标释放过程中即触发，此时工具箱仍在处理鼠标事件，`patchSticky` 覆盖/恢复的方法与工具箱已处理的状态不一致。改为 `mousedown` 设 `_toggleFromMouse` 标志，`change` 检测到标志后跳过（由 `mouseup` + `setTimeout(0)` 统一处理），确保只有鼠标完全松开后才处理分类选中/清除，彻底解决长按/快速点击导致的状态紊乱。

## [v0.5.0] - 2026-08-08

### 新增
- **「AI 工具」积木**：把一段积木逻辑注册为可被 AI 调用的工具函数。块上可填工具名、描述（AI 判断何时使用）、可选返回内容开关；下方 DO 区域放置工具逻辑，逻辑里可用「工具返回值」积木带出结果。运行时会经 AstrBot 的 `tool_loop_agent` 走 provider 工具调用机制：积木程序启用并处于开始时，`AI 回答` 会把可用工具列表一并发给模型，被选中工具实时执行并把结果喂回模型。
- **「收纳盒」开关**：工具箱底部新增「收纳盒常驻」开关（勾选图标样式，与程序启用/关闭开关一致）。开启后收纳盒 lid、收纳盒 flyout、工具箱弹出面板（`.blocklyToolboxFlyout`）以及工具箱分类选中状态均不会因点击工作区而丢失，实现完整「常驻」效果；关闭后恢复默认行为。从关闭切换为开启时，自动选中第一个分类（事件）并展开其 flyout。开关不影响收纳盒图标本身的显隐——无论开启还是关闭，收纳盒始终可见。选择状态保存在本地。同时修复 Blockly 内部通过 `sprites.svg` 渲染的收纳盒图标在插件页请求不带 `asset_token` 导致 401 的问题——改为在注入时把图标引用替换为内嵌 data URI。
- **右键菜单「注释」功能**：工作区空白处右键可「新建注释」（黄色便签、可直接编辑），积木上右键可「添加/删除注释」。Blockly 默认只注册积木上的注释菜单项，工作区空白处的「新建注释」漏掉注册，本版本显式补上。

### 修复
- **插件页在沙箱 iframe 中 localStorage 不可用报错**：AstrBot 插件页的 iframe 的 `sandbox` 属性可能缺少 `allow-same-origin`，导致页面初始化时访问 `localStorage` 直接抛出异常、Blockly 编辑器无法加载。已将所有 `localStorage` 调用包裹在 `try/catch` 中，沙箱环境下静默降级（收纳盒默认开启常驻状态）。
- **关闭常驻后收纳盒图标被隐藏**：常驻开关仅控制「粘性」行为，不应影响收纳盒图标的显隐。移除 `svgGroup.style.display` 设置，收纳盒始终可见，开关只控制 lid/flyout 是否自动关闭。

### 其他
- Blockly 注入开启 `comments` 选项，为右键注释功能提供前提。

## [v0.4.2] - 2026-08-08

### 新增
- **「AI 回答」块「指定模型」改为模型列表**：块上显示当前已选模型数量，点击齿轮按钮在弹窗内添加/排序/删除多个 `provider:model`（顺序即优先级、失败自动切换下一个）。进入 Blockly 页面时自动获取后端可用模型列表供选择；不指定时沿用程序「可用模型」白名单。模型列表随积木序列化保存。
- **「格式化文本创建」块**（文本分类）：点击齿轮按钮打开格式化弹窗——左侧多行编辑区、右侧标签区（可「创建标签」）；点击标签在编辑器光标处插入 `{标签}`。退出弹窗后，模板中的每个 `{标签}` 自动渲染为可连接文本块的输入端口，运行时把端口内容替换进对应标签位置。

## [v0.4.1] - 2026-08-08

### 修复
- **撤回/戳一戳/新成员加入事件不触发**：旧版画布上虽可放置多个事件入口积木，但保存时只记录第一个（通常是「当收到消息」），导致通知类事件匹配不到任何程序。现在：
  - 一个程序可监听多个事件类型（`event_type` 支持逗号分隔，如 `message,recall,poke`），画布上有几个事件入口积木就监听几种；
  - 后端加载/导入时自动从积木工作区推断事件类型，旧数据无需手工重存即可恢复多事件监听；
  - 每个事件入口积木生成独立的 `if _blk.event_type == '…':` 分支，程序内各事件逻辑互不干扰。
- 「事件类型」信息块改为返回本次执行对应的真实事件类型（此前返回程序配置，多个事件时无法判断当前实际类型）。
- 升级兼容：检测到旧版本（不含事件分支判断）生成的积木代码在通知事件中会误执行消息分支，此时跳过该程序并在日志提示「请在 WebUI 打开并保存一次」，防止误回复。
- **「输出日志」积木与 print 在 AstrBot 日志界面无显示**：积木运行时使用的是独立命名的 logger，AstrBot 只对 `astrbot` 命名空间的 logger 配置了输出 handler，独立 logger 的 INFO 会被静默丢弃。现在改用 AstrBot 主 logger，`输出日志`/print 的内容会出现在 AstrBot 日志中。
- **图片/表情/@/语音等非文本消息不触发程序**：AstrBot 中这类消息的 `message_str` 为空文本，旧逻辑在事件属性过滤前就把它当作"无消息"直接跳过。现在仅对纯文本执行前缀跳过，非文本消息交给触发条件与「消息属性过滤」判断（`任意` 时全部命中），保证积木能收到图片/表情/@/语音等消息。

### 优化
- 增加事件分派日志：通知事件到达、命中/未命中程序、旧代码跳过原因均会打印，便于排查事件为何未触发。
- **WebUI 深色主题中 Blockly 工具箱/工作区配色优化**：
  - 工具箱分类文字：新版 Blockly 用 HTML 渲染分类文字（`blocklyToolboxCategoryLabel`），此前误用旧版类名且依赖 `fill`，导致深色主题下文字颜色未生效、与背景对比度过低。现已对新旧类名统一设置 `color`/`fill`，浅色主题黑色、深色主题浅色；
  - 工具箱（收纳盒）、分类行悬停/选中、工作区画布、弹出积木面板（flyout）、滚动条背景统一改为深色，与页面主题一致。

## [v0.4.0] - 2026-08-08

### 新增
- **多种事件入口**：新增「当撤回消息」「当新成员加入」「当被戳一戳」事件块；程序按画布上的主事件块决定监听的事件类型（message / recall / member_increase / poke），后端据此分派。
- **「当接收到消息」消息属性选择**：事件块支持按内容类型过滤（任意/纯文本/图片/表情/@某人/语音/引用回复），只处理命中条件消息的执行。
- **消息盒子新块**：「消息类型」「包含图片」「包含表情」「包含@」。
- **事件信息块**：「事件类型」「消息ID」「目标ID」（戳一戳被戳者）、「操作者ID」（撤回者/邀请者）。
- **AI 回答块「指定模型」**：块上可填逗号分隔的 `provider:model` 列表（顺序即优先级），当前会话模型在列表头部时优先复用；单个模型请求失败自动切换下一个，全部失败返回聚合错误。未指定时沿用程序级「可用模型」白名单。
- **可用模型改为按钮弹窗**：属性栏不再展开长列表，点击按钮在弹窗内添加/排序/删除白名单模型，避免压缩 Blockly 编辑区。
- **测试运行独立弹窗**：从编辑器区移入弹窗，放大编辑区；测试时可选择模拟消息类型（文本/图片/表情/@/语音）。

### 修复
- 通知类事件（撤回/新成员加入/戳一戳）此前不会触发任何程序：平台将这类事件包装为消息事件但 `message_str` 为空，旧逻辑直接跳过。现在按原始事件类型正确分派到对应程序。
- `simulate_program` 改用 `time.monotonic()` 替换 `loop.time()`（3.10 起被弃用），消除弃用告警。

### 优化
- 程序代码执行前 AST 检查 + 编译改用 `functools.lru_cache` 缓存结果，不同程序共用编译结果，减少每事件重复编译开销。
- `BlocklyManager.enabled_programs()` 结果缓存，程序未变更时复用排序结果，降低每条消息的处理开销。

## [v0.3.9] - 2026-08-08

### 变更
- **导入同名冲突弹窗**：导入程序时只要与现有程序**名称相同**（不区分大小写，与 id 是否一致无关），不再静默覆盖/导入。后端 `api_import` 未提供策略时返回 `NAME_CONFLICT`，由 WebUI 弹窗逐条询问处理方式，支持「覆盖已有的同名程序 / 使用新命名」两种策略；`on_conflict` 键值由前端以 `{程序名: "overwrite" | "rename"}` 形式随请求返回。
- WebUI 导入改为直接读取本地 JSON（`api_import`）发送，不再经 `api_import_file` 上传接口。

### 修复
- **重复导入同名程序不再直接导入**：此前仅当「名称相同且 id 不同」才触发冲突确认，若导入文件与现有程序 id 相同（如同一导出文件反复导入）会静默覆盖、不弹窗。现在只要名称已存在即返回冲突列表弹窗。
- 一批导入多个同名条目时，`rename` 策略会结合已分配名称去重（如 `test (2)`、`test (3)`），不再出现同批重名互相覆盖。

### 其他
- `BlocklyManager.unique_name` 新增可选参数 `used_names`，使导入批内重命名与磁盘已有名称一起参与去重。

## [v0.3.8] - 2026-08-08

### 修复
- **导入程序报错** `TypeError("'coroutine' object is not iterable")`：`api_import` / `api_import_file` 直接返回了 `_import_data` 的协程对象（漏写 `await`），导致 AstrBot 响应层报错。已改为 `await` 后返回。

## [v0.3.7] - 2026-08-08

### 修复
- **积木生成的代码缩进错误**（`unexpected indent` / `unindent does not match any outer indentation level`）：「当接收到消息」事件块的生成器改用直接生成入口子树代码，不再给顶层代码整体追加 Blockly 缩进，包装成函数体后缩进正确。包含 `if/else`（如 `条件` 分类）或多条语句（块数量超过两个）的程序均可正常运行。
- **游离积木被误执行**：画布上未连接「当接收到消息」的积木（如单独摆放的「返回消息」块）仍会被生成并执行，导致未拖入事件块也会回复。现在只生成挂在事件块下的逻辑，游离块不再参与运行。
- **启用开关图标不显示**：「启用」状态图标 `IMG_CHECK_OK` 的内嵌 base64 与素材 `check_ok.png` 不一致，重新基于当前素材生成。

### 优化
- 积木面板（工具箱）分类标签颜色加深并加粗，暗色主题下更清晰可辨。

## [v0.3.6] - 2026-08-08

### 变更
- 消息监听默认优先级从 `100` 提高到 `1000`（远高于第三方插件默认值 `0`，仅次于 AstrBot 内置插件）。这样本插件会最先处理消息，`返回消息`/`停止事件传播` 积木劫持事件（设置 `STOP` 结果）后，AstrBot 调度器会跳过后续插件，实现"仅本插件处理该消息"。

## [v0.3.5] - 2026-08-08

### 修复
- **积木程序无法生成代码（code 一直为空）**：`registerPythonGenerator` 中所有语句块（`当接收到消息`、`回复`、`返回消息`、`传出消息`、`停止事件传播`、`发送`、`延时`、`输出日志`）的生成器错误地返回了值块格式的 `[代码, 优先级]` 数组，而 Blockly 要求语句块生成器返回纯字符串，导致 `workspaceToCode` 抛出 `Expecting string from statement block`。任何包含这些积木的程序保存时代码生成都会静默失败，`code` 字段始终为空、运行无效果。现在所有语句块生成器返回字符串。
- 积木代码生成失败时不再静默吞错：`generateCode` 异常会在控制台输出具体原因，便于定位。

## [v0.3.4] - 2026-08-08

### 修复
- 积木程序缺少已生成的 `code`（手写/旧版导入数据）时运行无效果且无日志：WebUI 加载到此类程序会自动从积木重新生成代码并静默保存；后端运行积木程序时 `code` 为空将给出明确错误日志而非静默跳过。

## [v0.3.3] - 2026-08-08

### 新增
- 启用开关「启用后」显示新的 `check_ok` 图标素材（侧栏与工具栏开关；未启用时仍显示灰度 check 图标）。

### 修复
- 刚进入插件页就误弹「切换程序：当前程序有未保存的修改，是否放弃？」：进入页面时 `setTheme` 触发的 Blockly `THEME_CHANGE` 等 UI 事件不再把工作区标记为已修改（仅忽略 UI 事件，实际积木增删改仍会正常标记）。

## [v0.3.1] - 2026-08-08

### 修复
- **Blockly 插件页图标无法加载 + 控制台报错**：AstrBot 插件页只会给 HTML/CSS/import 中的静态资源引用追加 `asset_token`，JS 运行时创建的 `<img>` 与 Blockly 内部的媒体请求（`vendor/media/*.mp3`、`sprites.svg` 等）不携带 token 导致 401（声音资源还触发跨域 `Failed to fetch`）。处理方式：
  - 侧栏复制/删除/启用开关、模型白名单移除等运行时创建的图标改用内嵌 data URI，不再依赖图片文件请求。
  - Blockly 关闭 `sounds`（消除 `fetch` 报错）、关闭 `trashcan` 与缩放控件（不再请求 `sprites.svg`），缩放滚轮功能保留。

## [v0.3.0] - 2026-08-08

### 变更
- 移除积木/代码编辑模式切换：程序在新建时选定编辑方式，之后相互隔离、不可中途切换。
- 「可用模型」白名单改为 **提供商 ID + 模型 ID**（格式 `provider_id:model`），支持跨提供商切换模型，并兼容旧的纯模型名格式。

### 新增
- 侧栏折叠图标改为自绘 SVG chevron，折叠/展开时动态旋转变化。
- 侧栏程序列表文字对比度提升（元信息加深、选中项高亮、操作按钮常显）。
- 集成新图标素材：`check.png`（启用开关）、`copy.png`（复制）、`delete.png`（删除/移除）。
- 积木程序空工作区时自动加载默认「当接收到消息」事件块。

### 修复
- **沙箱逃逸（安全）**：`exec` 前新增 AST 静态检查，阻断 `import`、`__` 魔术属性/名称、`global/nonlocal`、类定义等逃逸路径（如 `().__class__` 链）。
- 测试运行结果错误样式失效（`.test-result .err` 后代选择器不生效），改为容器加 `.err` 并逐行渲染错误行。
- 删除当前程序后表单残留未清空；现在会清空并禁用编辑器，删除后自动选中第一个程序。
- 触发值在非 contains/prefix/regex 触发条件下残留。
- 程序列表接口误传超大 workspace/code 字段，改为轻量摘要。
- 加载可用模型列表成功时不再弹出冗余提示。

## [v0.2.0] - 2026-08-08

### 新增
- 程序级「可用模型」白名单（`AI 回答` 积木的模型授权）。
- 新建程序弹窗：命名去重 + 选择积木/代码模式。
- 程序名称重复时后端自动追加序号（如 `xx (2)`）。
- 侧栏折叠、重置按钮、程序图标素材。

### 变更
- **传出/返回消息由积木决定**：移除程序级 `mode` 字段，由 `返回消息`（劫持并回复）/`传出消息`（放行给 AstrBot）/`回复`（发送但不劫持）积木单独决定行为。
- 事件传播仅在 `event.is_stopped()` 时中断后续程序。

### 修复
- **UI 交互失效（无法删除/无法切换程序）**：插件页面 iframe 沙箱不含 `allow-modals`，原生 `confirm()` 静默失败；全部替换为自定义弹窗。
- **Blockly 积木报错**：`当接收到消息` 块缺少 `%1` 占位符导致参数引用错误。
- 插件加载失败：`/programs/<pid>/test` 路由引用了不存在的处理器（`api_simulate_program` → `api_test_program`）。

## [v0.1.0] - 2026-08-08

### 新增
- 基于 Blockly 的可视化编程：积木自动生成 Python 代码。
- 多程序管理：新建、复制、删除、导入、导出、开启/关闭、优先级、触发条件。
- 受限执行沙箱：白名单 builtins + 独立超时；提供 `event`/`ctx`/`_blk` 对象。
- AstrBot 专属积木：返回/回复/传出/停止事件、主动发送、调用 AI、HTTP 请求、日志、延时等。
- 独立 WebUI 编辑器（积木模式 + 代码模式）与 `/blockly` 聊天指令。
